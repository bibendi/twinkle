= form_for [@client, @channel], html: { class: "form form-horizontal channel" } do |f|
  .form-group
    = f.label :name, class: 'control-label col-lg-2'
    .col-lg-10
      = f.text_field :name, class: 'form-control', required: true
    = f.error_span(:name)
  .form-group
    = f.label :active, class: 'control-label col-lg-2'
    .col-lg-10
      = f.text_field :active, class: 'form-control', required: true
    = f.error_span(:active)
  .form-group
    .col-lg-offset-2.col-lg-10
      = f.submit nil,class: 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), client_channels_path(@client), :class => 'btn btn-default'
