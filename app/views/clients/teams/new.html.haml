- breadcrumb :client_teams, @client

.page-header
  %h1= t '.title', default: [:'helpers.titles.new', 'Add %{model}'], model: Team.model_name.human.titleize

= form_for @client_team, url: client_teams_url(@client), html: {class: "form form-horizontal channel"} do |f|
  = render "form", f: f
